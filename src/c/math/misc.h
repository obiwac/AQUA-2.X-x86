
#ifndef __AQUA__MATH_MISC_H
	#define __AQUA__MATH_MISC_H
	
	#include "../types.h"
	#include "const.h"
	#include "pow.h"
	#include "round.h"
	
	float exp(float x);
	float fmod(float x, float y);
	
	int min(int a, int b);
	int max(int a, int b);
	
#endif
